import { View, Text } from 'react-native'
import React from 'react'
import ListForAllTypes from '../components/ListForAllTypes'

const Responded = (props) => {
  return (

    <ListForAllTypes  {...props} />
  )
}

export default Responded